// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("xxtea", "for xxtea support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("xxtea", "enable xxtea support", "no");

if (PHP_XXTEA != "no") {
	EXTENSION("xxtea", "xxtea.c");
}

